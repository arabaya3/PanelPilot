# Adan's lane — unattended run log

One row per task, appended as each merges. Same format as `ayed-lane-log.md`.

| Timestamp        | Task   | Outcome         | Tokens | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------- | ------ | --------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-27 09:15 | BE-005 | Merged (PR #47) | ~118k  | Scope grew well past "crawler jobs": the task assumed text extraction was solved, but nothing in the repo produced the `StructureMap` chunking needs and no PDF library was declared. Delivered robots.txt compliance, per-brand crawlers with domain-boundary host matching, content-hash change detection, a staging pipeline holding no index capability, and `app/ingestion/structure.py`. pdfplumber (MIT) chosen over PyMuPDF (AGPL, disqualifying here) after probing that it surfaces per-character font size and ruling-line table detection rather than flat text. Three review rounds; rounds 1-2 were each a one-dimensional threshold tightening that opened a symmetric failure on the other side, so round 3 switched to signals actually present in the data. **Open limitation:** a table continued across a page break with neither a repeated header nor a "(continued)" banner stays two blocks. Geometry was measured as a candidate signal and rejected — it does not separate the two cases. Recorded in the tracker rather than papered over.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-27 09:30 | BE-006 | Merged (PR #49) | ~46k   | Alerts on staleness of the last success rather than emptiness of the last result — the two are identical from outside, which is the whole problem. Two rules from the task's alert-fatigue requirement: a single failure never alerts, and never-succeeded is reported separately from stale. One exception: a source never _checked_ alerts immediately, since patience is only owed where a retry is actually coming. Verified by mutation — 7 mutants, each killed by its own test, including the flapping source that neither rule catches alone. Not yet wired into a crawl run; `crawl_source` has no production caller until BE-007. A stray `TypeError` here turned out to be SQLite discarding timezones where Postgres does not, fixed in the fixture rather than by making the domain defensive about a database the product never uses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-27 10:05 | AI-012 | Merged (PR #51) | ~52k   | Three labels, a reviewable rubric in `docs/verification-rubric.md`, and the escalation routing rule as code so it cannot drift from the document. `uncertain` is a first-class outcome rather than a soft `incorrect`: an engineer forced to choose on an item they cannot judge will sometimes guess, and a guessed `correct` is indistinguishable from a verified one afterwards. Built AI-012 before BE-007 despite the tracker order, because BE-007 encodes this vocabulary and building it second would have meant designing the label set twice. On the inter-rater criterion: a test cannot conscript two engineers, and two mocks both returning the fixture's expected label would be the fixture agreeing with itself — so what is checked is the property that makes agreement possible, that every calibration item is settled by a named rubric clause which exists as a heading in the document. The human half stays a rollout gate. Mutation-verified: renamed heading, mislabelled item, dropped escalation, fourth label.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-08-27 10:10 | BE-007 | Merged (PR #52) | ~104k  | Chunk-level queue, daily assignment, atomic claiming, lead-only escalation view. **The concurrency test was worthless as first written and had to be rebuilt.** `ThreadPoolExecutor.map` alone never overlaps the threads — each claim is one fast statement — so it passed against a deliberately broken read-check-write implementation. A barrier releasing all twelve contenders after each opens its transaction now makes eleven of twelve win the same item against the naive version, and exactly one against the real one. Without that it would have shipped as false assurance on the task's single most important property. Two things deliberately not overclaimed: `SKIP LOCKED` was measured under forced overlap and changes no outcome (correctness is the row lock; it only stops a retried run waiting), and that is now written down rather than implied; and the migration needed a `server_default` on `status` to backfill existing rows, verified by downgrade/insert/upgrade. CI gained a Postgres service plus a skip guard mirroring the OpenSearch one — without it all 21 tests would skip silently while the job went green. Confirmed in CI: 21 ran. Also regenerated the shared TypeScript types, which the drift check caught as stale.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-27 10:20 | AI-013 | Merged (PR #54) | ~58k   | The seam joining the crawler's staging write to the verification queue. Lives in `app/domain` rather than either module it joins: `app/ingestion` holds no capability to write anywhere and the queue has no business knowing what a crawl is. A test caught a real bug in the burst cap — slicing the input took the same first N ids every run, so a later run re-presented them, the idempotent skip absorbed them, and the tail beyond the cap was never reached. "Deferred" chunks would have been permanently lost. The cap now applies to what a run adds rather than what it is offered. A chunk body with no usable id is refused rather than skipped, for the same reason: a chunk that cannot be queued is content no verifier will ever see. Mutation-verified 3/3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-08-27 10:26 | AI-014 | Merged (PR #55) | ~71k   | Flag endpoint capturing what the user was looking at, routed into the queue tagged post-launch. The context is copied at flag time, never re-derived — retrieval over a growing index returns different passages for the same question later, so a reviewer handed a fresh retrieval would judge an answer the user was never given and have no way to tell. Question, answer, and full passage text are copied onto the flag rather than read through `turn_id`, because retention prunes sessions long before an accuracy problem is fixed; the FK is SET NULL so the link goes and the evidence stays. Both endpoint failure modes report 404 so a caller cannot probe for other tenants' turn ids. Mutation-verified 5/5.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-27 10:35 | AI-009 | Merged (PR #56) | ~96k   | Parser-backed PLC validation. Library chosen on investigation: blark is the one real Python ST parser and is GPL — same disqualification that ruled out PyMuPDF for BE-005; plcopen is MIT but handles PLCopenXML interchange files, not ST source. So the grammar is written against lark (MIT), the engine blark itself uses. Anything unparseable reports INCOMPLETE, never VALID, and unsupported constructs are matched before parsing so valid code is reported as unchecked rather than failed for using language the checker has not implemented. Ladder returns structured rungs validated through their ST equivalent; an NC contact becomes `NOT tag`, since rendering it as a plain read would invert the logic while still parsing. 12 broken programs all flagged, 5 valid ones none false-flagged, 7 unsupported all INCOMPLETE. Mutation-verified 6/6 — treating INCOMPLETE as ready fails 8 tests. Also fixed the Postgres fixtures, which were order-dependent, and restored a CI entry for the AI-014 tests that was lost in a merge; those 15 tests had been skipping silently on main.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-27 11:05 | BE-010 | Merged (PR #57) | ~62k   | Two thin endpoints over AI-009. An architecture test caught the first attempt importing `app.ai` from a route; the policy moved to `app/domain/plc.py`, where it belonged anyway — "a validator that raises has not passed anything" is a property of the domain call, not of HTTP. Any exception on the checking path becomes an explicit INCOMPLETE verdict that is never `ready`, distinguishable by `checked_by` from the checker's own unsupported-dialect answer: both untrusted, but one is a known gap and the other a defect. The catch is deliberately broad because a grammar can recurse too deeply on adversarial input. Generation refuses rather than stubbing. Putting `PlcValidationResult` into shared-types surfaced a real collision — a second `Severity` schema made the generator namespace both and broke the frontend's existing import, renamed to `FindingSeverity`. That is exactly the drift the shared-types requirement exists to prevent, caught before merge. Mutation-verified 3/3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-27 11:20 | FE-009 | Merged (PR #59) | ~88k   | Ladder as SVG rungs, ST syntax-highlighted with findings on their lines, and a verdict banner that blocks the looks-done impression. The ladder schema had to grow first: AI-009 modelled a rung as contacts in series, which cannot express a seal-in — the most common rung in the trade — while FE-009 explicitly requires branches and timer blocks. Added `LadderBranch`/`LadderBlock`; the ST renderer maps series to AND, parallel to OR, NC to NOT, and parenthesises branches, without which a seal-in reads as `Start OR (MotorRun AND NOT Stop)` and latches on forever. Hand-written tokeniser rather than Shiki: Shiki ships a WASM regex engine and a grammar loader for a hundred languages, and the real cost was a second grammar to keep in step with the validator's. **Two branch tests had to be rewritten** — they counted lines with `x1 === x2`, which the power rails also satisfy, and a mutant that moved one endpoint a single pixel passed while drawing a collapsed branch. Now measured against the paths' own rows; kills all three branch mutants.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-27 11:35 | FE-012 | Merged (PR #61) | ~64k   | The verification console. Split-pane, because a verifier who opens the manual in another tab is checking against memory, which is not verification. Three separated actions rather than a segmented control: the failure worth designing against is clicking through on autopilot. The note requirement is a pure function rather than a JSX condition — a rule that holds only inside the component is one a second submit path would bypass — and whitespace does not count, since a space satisfies HTML `required` and tells a lead nothing. The claim race believes the server and distinguishes a lost race from a failed request, because one means move on and the other means try again. An item with no source says so rather than showing a blank frame: a verifier cannot otherwise tell a failed load from a genuinely absent source, and one of those means they must not label it `correct`. Mutation-verified 4/4.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-27 11:50 | FE-013 | Merged (PR #62) | ~41k   | Audited every customer-facing surface at 360px and 768px, checklist in `docs/responsive-audit.md`. Three real defects: image previews were height-capped but not width-capped, so a wide cabinet photo rendered 160px tall and 800px wide and pushed the page sideways; order codes could not wrap, since `3RV2011-1JA10-0BA0` is one word to CSS; and user message bubbles had the same problem, because the 80% cap bounds the bubble rather than the text inside it. Added `check:responsive` to the lint run — static rather than rendered, because jsdom performs no layout and a test asserting "nothing exceeds 360px" there would pass whatever the markup said, which is worse than no test. Verified against its own failure modes: three bad patterns caught, two legitimate ones passed. The BOM table is out of scope because it does not exist (BE-011/FE-010 blocked), but the script already refuses an unwrapped table, so one added later cannot merge without a fallback. Also fixed a prettier failure CI caught that my apps/web-scoped check had missed — CI formats from the repo root.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-28 20:10 | Bugfix | Merged (PR #74) | ~120k  | Reported symptom: the stream emitted `retrieving` then stopped with no terminal event. The log named it exactly — `RequestError(400, 'illegal_argument_exception', 'Pipeline panelpilot-hybrid-symptom_description is not defined')` — so it was neither the known empty-index case nor a Voyage regression: `symptom_description` is a real `QueryType`, and no `EmbeddingError` appeared, placing the fault downstream of embedding. **Two defects.** First, `ensure_index` is the only thing that registers the indices and the per-query-type blend pipelines, and grep found it had no production caller at all — `GET /_search/pipeline` returned `{}` and no `panelpilot-*` index existed, so every hybrid query named a pipeline OpenSearch rejects with a hard 400. Fixed with an explicit bootstrap entry point in the dev-only compose `command:`, the same slot as `alembic upgrade head`; not the Dockerfile CMD (production image) and not a lifespan hook, which would couple app boot to a service and let a query create an index from whichever mapping the first caller reached. Second, `stream_diagnosis` had no error handling, and could not usefully have raised anyway: a generator body runs only after `StreamingResponse` has sent 200 and the SSE headers, so the exception aborted the body rather than becoming a status — the client could not distinguish it from a dropped connection. Failures now yield `refused` + `result`; an invented `error` event would have been _worse_, since the frontend ignores unknown event names by design. No quota charged for a turn that produced nothing, and the exception text stays in the log. Verified on the live stack, not just in tests: bootstrap creates all four pipelines and both indices at container start, the original query completes end-to-end, and deleting the pipeline again to recreate the exact fault yields a clean refusal instead of a dead stream. 12/12 mutants killed, including `recreate=True` — which would have silently emptied production on every container restart.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-29 00:45 | FE-011 | Merged (PR #76) | ~150k  | The endpoint FE-011 was blocked on, plus the sidebar. `GET /api/v1/sessions?cursor=`, cursor-paginated, ordered by last activity, scoped by tenant, reusing BE-014's storage. Three decisions carry the work. **Scoped by tenant, not user id** — a trial session is created before anyone signs up, so `user_id` is null on exactly the conversations a new engineer most wants to find again. **Ordered by the newest turn, not the session row's `updated_at`** — appending a turn inserts into `diagnostic_turns` and never writes to `diagnostic_sessions`, so that column still holds when the conversation was _opened_; sorting by it would push a session worked on all afternoon below one opened yesterday and abandoned. **The cursor carries the row id as well as the timestamp** — timestamp alone is not a total order, and a keyset cursor over a non-unique key silently repeats or skips rows at the page boundary. Mounted outside the diagnostics prefix so the trial rate limit does not throttle a sidebar fetch. **One schema change, forced by the acceptance criterion**, which asks for the _context indicator_ restored and not just the messages: that indicator reads `StructuredDiagnosis.equipment_model`, which lived only on the live response, so a replayed turn came back with the chip blank — verified directly, `_replayed_diagnosis` returned `equipment_model=None` for every stored turn. Re-deriving it from the stored prose would mean guessing a model number out of an answer, which is the one thing the chip's neutral state exists to prevent, so the turn now records what was shown. A column on an existing table, not a new one. Frontend follows `trial.ts`: a discriminated union rather than a throw, every field narrowed rather than cast, and "no history" told apart from "history failed to load" — identical to the reader, and only one means their conversations are gone. Verified on the running stack, not just in tests: second tenant sees none of the first's rows, 401 unauthenticated, 422 on a malformed cursor and an out-of-range limit with a clean error rather than a traceback, and `limit=2` walked 6 sessions over 3 pages with no repeats and no gaps. 28/28 mutants killed. One survived the first pass — the page-size cap, whose test asserted a row count over a handful of rows and held whether or not the cap existed; rewritten to read the LIMIT the query actually carries. Also found and fixed a stale README paragraph describing the old `retrieving`-then-silence behaviour that PR #74 had already changed. |
